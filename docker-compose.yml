version: '2'

services:
  nginx:
    build: ./nginx
    depends_on:
      - web
    restart: always
    volumes:
      - "./nginx/sites-enabled:/etc/nginx/sites-enabled"
      - "./scn_js:/scn/scn_js"
      - "./scn_docs/build/:/scn/docs"
      - "$DATASET_PATH:/var/datasets"
    ports:
      - "3133:80"

  genequery:
    depends_on:
      - fs
    restart: always
    build: ./genequery
    expose:
      - 8423
    ports:
      - "8433:8423"
    volumes:
      - "$GMT_PATH:/genequery/data-files"


  mongo:
    image: mongo:latest
    restart: always
#    ports:
#      - "27033:27017"
    volumes:
      - "./mongo/init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro"
      - "$DATASET_PATH:/var/datasets"
  fs:
    env_file:
      - .env
    restart: always
    depends_on:
      - mongo
    build: ./scn_fs
    volumes:
      - "$DATASET_PATH:/var/datasets"
      - "$GMT_PATH:/vat/gmt"
      - "$TMP_PATH:/var/tmp"
    command: ["java", "-Djava.library.path=./", "-jar", "fs.jar", "/var/datasets", "/vat/gmt", "All Files/test_dir", "$BOX_DEV_KEY"]

  web:
    env_file:
      - .env
    restart: always
    depends_on:
      - fs
    build: ./scn_server
    volumes:
      - "$DATASET_PATH:/var/datasets"
      - "./scn_js:/scn/scn_js"
    command: ["java", "-Djava.library.path=./", "-jar", "server.jar"]
